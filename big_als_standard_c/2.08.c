#include <stdio.h>

int main(void) {

	// declare x and y
	int x;
	int y;

	// use the uninitialized variables
	printf("The value of x is %d", x);
	printf(" and the value of y is %d.", y);

	return 0;
}
